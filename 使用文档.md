# 引导页 + 网关系统使用文档

本文档用于说明当前项目的完整使用方式，包括：
- 引导页（行星展示交互）
- 网关跳转页
- 后端服务注册与心跳机制
- 本地调试能力

---

## 1. 项目概览

当前项目由三部分组成：

- `index.html`
  - 服务引导主页
  - 以“恒星 + 行星”方式展示服务
  - 点击行星可置前，再次点击可跳转到网关
  - 支持本地调试滑条（球大小、轨道大小、大球大小）

- `gateway.html`
  - 网关跳转页
  - 读取 URL 参数 `service`
  - 调后端查询服务目标地址并跳转

- `server.js`
  - 轻量后端 API（Node.js 原生 `http`）
  - 提供服务查询、服务自注册、心跳上报等能力
  - 数据持久化文件：`data/services.json`

---

## 2. 快速启动

### 2.1 启动后端

在项目目录执行：

```bash
npm start
```

默认监听端口：`8787`

启动成功后可访问健康检查：

```bash
curl http://localhost:8787/api/health
```

### 2.2 服务器打包运行（推荐）

以下为生产/测试服务器常用部署方式：`Nginx + PM2 + Node.js`。

#### 步骤 1：本地打包

在项目目录执行：

```bash
tar -czf heq-portal.tar.gz index.html gateway.html server.js package.json 使用文档.md
```

#### 步骤 2：上传并解压到服务器

示例（按你的用户名/服务器地址替换）：

```bash
scp heq-portal.tar.gz user@your-server:/opt/heq-portal/
```

服务器执行：

```bash
cd /opt/heq-portal
tar -xzf heq-portal.tar.gz
mkdir -p data
```

#### 步骤 3：安装依赖并启动后端

```bash
cd /opt/heq-portal
npm install
PORT=8787 REGISTER_KEY=your_key HEARTBEAT_TTL_MS=30000 pm2 start server.js --name heq-gateway
pm2 save
pm2 startup
```

检查后端是否正常：

```bash
curl http://127.0.0.1:8787/api/health
```

#### 步骤 4：Nginx 反代与静态页托管

新建 Nginx 站点配置（例如：`/etc/nginx/conf.d/heq-portal.conf`）：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /opt/heq-portal;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

重载 Nginx：

```bash
nginx -t && systemctl reload nginx
```

#### 步骤 5：前端 API 地址建议

当前前端使用固定 `http://localhost:8787`。  
若部署到线上，建议将 `index.html` 与 `gateway.html` 中的 `API_BASE_URL` 改为相对路径：

```js
const API_BASE_URL = "";
```

这样会自动请求同域名下的 `/api/...`，与 Nginx 反代搭配最佳。

#### 步骤 6：后续更新发布

每次发布建议流程：

1. 本地重新打包
2. 上传并解压覆盖
3. 重启后端：`pm2 restart heq-gateway`
4. 重载 Nginx：`nginx -t && systemctl reload nginx`

---

## 3. 页面访问方式

### 3.1 引导页

打开：

- `index.html`

建议通过本地静态服务器打开（也支持 `file://` 本地文件方式）。

### 3.2 网关页

格式：

- `gateway.html?service=服务编码`

示例：

- `gateway.html?service=cloud-drive`

---

## 4. 数据文件说明

后端会自动创建：

- `data/services.json`

首次初始化时会写入 3 个测试服务（已在代码注释中标明），后续可自行删除或替换。

---

## 5. 后端接口说明

统一返回格式：

```json
{
  "code": 0,
  "msg": "说明信息",
  "data": {},
  "timestamp": "2026-01-01T00:00:00.000Z"
}
```

---

### 5.1 健康检查

- 方法：`GET`
- 路径：`/api/health`

---

### 5.2 查询全部服务

- 方法：`GET`
- 路径：`/api/services`

返回每个服务包含状态字段：
- `isOnline`：是否在线
- `lastSeenAt`：最后心跳时间戳（毫秒）

---

### 5.3 查询单个服务

- 方法：`GET`
- 路径：`/api/services/:slug`

---

### 5.4 服务自注册（幂等）

- 方法：`POST`
- 路径：`/api/services/register`
- 说明：
  - 服务启动时主动调用
  - 若 `slug` 已存在则更新，不存在则新增

请求体示例：

```json
{
  "name": "订单服务",
  "slug": "order-service",
  "description": "订单创建与查询",
  "targetUrl": "https://order.example.com",
  "tag": "业务服务",
  "tone": "indigo"
}
```

---

### 5.5 心跳上报

- 方法：`POST`
- 路径：`/api/services/heartbeat`
- 说明：
  - 服务运行中定时调用（建议 10 秒一次）
  - 超过 TTL 未上报则被判定为离线

请求体示例：

```json
{
  "slug": "order-service"
}
```

---

### 5.6 删除服务

- 方法：`DELETE`
- 路径：`/api/services/:slug`

---

## 6. 鉴权（可选）

你可以通过环境变量启用注册/心跳密钥校验：

- `REGISTER_KEY`

启用后：
- `/api/services/register`
- `/api/services/heartbeat`

都需要请求头：

- `x-register-key: 你的密钥`

---

## 7. 环境变量

- `PORT`
  - 后端端口，默认 `8787`

- `REGISTER_KEY`
  - 自注册/心跳密钥（可选）

- `HEARTBEAT_TTL_MS`
  - 心跳离线超时时间（毫秒）
  - 默认 `30000`

---

## 8. 服务接入推荐流程

建议每个业务服务按以下方式接入：

1. 启动时调用 `/api/services/register`
2. 启动后循环调用 `/api/services/heartbeat`（建议 10 秒）
3. 停止时可不处理，超时后自动判离线

---

## 9. 示例：curl 接入

### 9.1 注册

```bash
curl -X POST "http://localhost:8787/api/services/register" \
  -H "Content-Type: application/json" \
  -d '{
    "name":"订单服务",
    "slug":"order-service",
    "description":"订单创建与查询",
    "targetUrl":"https://order.example.com",
    "tag":"业务服务",
    "tone":"indigo"
  }'
```

### 9.2 心跳

```bash
curl -X POST "http://localhost:8787/api/services/heartbeat" \
  -H "Content-Type: application/json" \
  -d '{
    "slug":"order-service"
  }'
```

### 9.3 使用 `service-register.js` 通用 SDK（推荐）

项目已提供通用接入文件：`service-register.js`。  
可实现“启动时自注册 + 定时心跳 + 简单重试 + 优雅停止”。

示例：

```js
const { createServiceRegisterClient } = require("./service-register");

const client = createServiceRegisterClient({
  apiBaseUrl: "https://638rember.me/api",
  registerKey: process.env.REGISTER_KEY,
  name: "订单服务",
  slug: "order-service",
  description: "订单创建与查询",
  targetUrl: "https://order.example.com",
  tag: "业务服务",
  tone: "indigo",
  heartbeatIntervalMs: 10000
});

client.start().catch((err) => {
  console.error("注册启动失败:", err.message);
  process.exit(1);
});

process.on("SIGINT", () => {
  client.stop();
  process.exit(0);
});
```

---

## 10. 前端调试面板说明（仅本地）

`index.html` 右下角有本地调试面板（仅以下环境显示）：

- `file://`
- `localhost`
- `127.0.0.1`

可调参数：
- 球大小
- 轨道大小
- 大球大小

滑条范围当前为：
- `0.5 ~ 5`

---

## 11. 常见问题排查

### 11.1 行星不显示 / 只有大球

排查顺序：

1. 后端是否启动：`npm start`
2. `GET /api/services` 是否有数据
3. 浏览器控制台是否有请求错误

---

### 11.2 服务显示离线

确认：

1. 服务是否在定时调用 `/api/services/heartbeat`
2. `slug` 是否与注册时一致
3. `HEARTBEAT_TTL_MS` 是否过短

---

### 11.3 注册失败（401）

说明启用了 `REGISTER_KEY`，但请求头未携带或不匹配：

- `x-register-key`

---

## 12. 维护建议

- 测试服务仅用于本地演示，生产请及时清理
- 定期备份 `data/services.json`
- 如果服务数量很大，建议后续把数据存储迁移到数据库
- 如果要多实例部署，建议把心跳状态存入 Redis（避免单机内存状态丢失）

---

## 13. 后续可扩展方向

- 服务分组（按业务域/环境分层）
- 权限控制（按角色显示服务）
- 心跳附带版本、延迟、负载等指标
- 网关跳转前记录访问审计日志

